{
    "Common": {
        "ApplicationInsights": {
            "ConnectionString": "$(SHARED_APP_INSIGHTS_CONNECTION_STRING)"
        },
        "AzureKeyVault": {
            "VaultUri": "$(SHARED_KEY_VAULT_URI)"
        },
        "Cors": {
            "AllowedOrigins": [
                "http://localhost:3000",
                "http://localhost:5000"
            ]
        }
    }
}